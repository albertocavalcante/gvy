#!/usr/bin/env bash
# Repin Maven dependencies
#
# NOTE: Lock file is currently DISABLED due to GMM checksum inconsistency.
# See MODULE.bazel for details and https://github.com/albertocavalcante/gvy/issues/1029
# This script is kept for future use when the issue is resolved upstream.
#
# Usage:
#   ./tools/repin  # Update maven_install.json (when lock file is enabled)

set -euo pipefail

echo "WARNING: Lock file is currently disabled in MODULE.bazel"
echo "Re-enable lock_file config before running this script."
echo ""
echo "To enable: uncomment lock_file and fail_if_repin_required in MODULE.bazel"
exit 1
