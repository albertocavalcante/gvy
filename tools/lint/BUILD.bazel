"""Linting tools for the Groovy Language Server project."""

# NOTE: The aspect_rules_lint API has changed and these symbols are no longer available.
# The linting setup needs to be updated to use the new API.
#
# TODO: Update linting configuration to use aspect_rules_lint v1.13.0 API
# See: https://github.com/aspect-build/rules_lint/blob/main/docs/linting.md
#
# For now, ktlint is disabled. To re-enable:
# 1. Check the correct API in the aspect_rules_lint documentation
# 2. Set up proper linting aspects in tools/lint/linters.bzl
# 3. Configure .bazelrc with linting aspects

# load("@aspect_rules_lint//lint:buf.bzl", "buf_toolchain")  # API changed
# load("@aspect_rules_lint//lint:eslint.bzl", "eslint_binary")  # Requires npm setup
# load("@aspect_rules_lint//lint:ktlint.bzl", "ktlint_binary")  # API changed

# ESLint binary for TypeScript linting (disabled until npm is configured)
# eslint_binary(
#     name = "eslint",
#     visibility = ["//visibility:public"],
# )

# ktlint binary for Kotlin linting (disabled until API is updated)
# ktlint_binary(
#     name = "ktlint",
#     visibility = ["//visibility:public"],
# )

# buf toolchain for Protocol Buffers linting (disabled until API is updated)
# buf_toolchain(
#     name = "buf",
#     visibility = ["//visibility:public"],
# )
