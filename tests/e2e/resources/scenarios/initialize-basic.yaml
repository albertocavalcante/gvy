name: initialize-basic
description: Basic lifecycle handshake that exercises initialize/shutdown in-process.
server:
  mode: inProcess
workspace:
  fixture: empty-workspace
steps:
  - initialize: {}
  - initialized: {}
  - shutdown: {}
  - exit: {}
