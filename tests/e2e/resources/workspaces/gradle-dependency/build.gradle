plugins {
    id 'groovy'
}

repositories {
    flatDir {
        dirs 'libs'
    }
}

dependencies {
    // Local test library with source JAR for E2E definition testing
    implementation name: 'testlib'
    implementation localGroovy()
}
