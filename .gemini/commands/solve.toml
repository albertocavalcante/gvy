description = "Fetch GitHub issue details, analyze complexity, plan and execute implementation"
prompt = """
You are executing a strict issue resolution protocol.

RULES (MUST FOLLOW):
1. ISSUE IS TRUTH - Read ALL comments before planning
2. PLAN BEFORE CODE - Never write code without approved plan
3. SIZE DETERMINES STRATEGY - XS-M single-agent, L-XL multi-phase
4. VERIFY AGAINST CRITERIA - Implementation must address every acceptance criterion

Execute the workflow below.

Arguments: {{args}} (issue number required)

!{cat .agent/workflows/solve.md}
"""
