--- a/private/rules/coursier.bzl
+++ b/private/rules/coursier.bzl
@@ -1144,6 +1144,14 @@
 
         if _is_unpinned(repository_ctx):
             artifact.update({"file": _relativize_and_symlink_file_in_coursier_cache(repository_ctx, artifact["file"], coursier_cache_path)})
+        else:
+            # Pinned case: Coursier downloaded to local v1/ cache with --cache v1
+            # but COURSIER_CACHE env var was set to absolute path, so JSON has absolute paths.
+            # Extract the relative path starting from v1/
+            file_path = artifact.get("file")
+            if file_path and "/v1/" in file_path:
+                relative_path = "v1/" + file_path.split("/v1/", 1)[1]
+                artifact.update({"file": relative_path})
 
         # Coursier saves the artifacts into a subdirectory structure
         # that mirrors the URL where the artifact's fetched from. Using
